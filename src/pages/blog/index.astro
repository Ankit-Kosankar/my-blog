---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';
import { getCollection } from 'astro:content';
import FormattedDate from '../../components/FormattedDate.astro';
import { Image } from 'astro:assets';
import { getMediumPosts} from '../../utils/getMediumPosts';
import { parseISO, parse } from 'date-fns';

const POSTS_PER_PAGE = 5;
const page = Number(Astro.url.searchParams.get('page') || 1);

function parseBlogDate(dateStr: string | Date) {
  if (dateStr instanceof Date) return dateStr;
  if (typeof dateStr !== 'string') return new Date();

  // Try ISO first
  const isoDate = parseISO(dateStr);
  if (isoDate instanceof Date && !isNaN(isoDate.valueOf())) {
    return isoDate;
  }
  // Try 'MMM dd yyyy'
  const customDate = parse(dateStr, 'MMM dd yyyy', new Date());
  if (customDate instanceof Date && !isNaN(customDate.valueOf())) {
    return customDate;
  }
  // Fallback: now
  return new Date();
}

// Fetch local posts
const allLocalPosts = await getCollection('blog');
console.log('🔍 Debug: Found', allLocalPosts.length, 'local posts');

// Extract unique categories
const categoryCounts = new Map<string, number>();
allLocalPosts.forEach(post => {
	const category = post.data.category;
	if (category) {
		categoryCounts.set(category, (categoryCounts.get(category) || 0) + 1);
	}
});
const sortedCategories = Array.from(categoryCounts.entries())
	.sort((a, b) => b[1] - a[1]);

// Get filter from URL
const categoryFilter = Astro.url.searchParams.get('category') || '';

const localPosts = allLocalPosts
	.filter(post => {
		if (!categoryFilter) return true;
		return post.data.category?.toLowerCase() === categoryFilter.toLowerCase();
	})
	.map(post => {
		const link = `/blog/${post.id.replace(/\.mdx?$/, '')}/`;
		console.log('🔗 Debug: Post', post.data.title, '->', link);
		return {
			id: post.id,
			title: post.data.title,
			description: post.data.description,
			link: link,
			pubDate: parseBlogDate(post.data.pubDate),
			source: 'local',
			category: post.data.category,
			type: post.data.type,
			tags: post.data.tags || [],
		};
	});

// Create a map for quick lookup
const localPostsMap = new Map(localPosts.map(p => [p.link, p]));

// Prepare search data for client-side search
const searchData = JSON.stringify(
	allLocalPosts.map(post => ({
		id: post.id,
		title: post.data.title,
		description: post.data.description,
		tags: post.data.tags || [],
		category: post.data.category || '',
		type: post.data.type || '',
		link: `/blog/${post.id.replace(/\.mdx?$/, '')}/`,
	}))
);

// Fetch Medium posts
const mediumPosts = await getMediumPosts(5);

// Combine and sort
const allPosts : any = [...localPosts, ...mediumPosts].sort(
  (a, b) => b.pubDate.valueOf() - a.pubDate.valueOf()
);

// Pagination
const totalPages = Math.ceil(allPosts.length / POSTS_PER_PAGE);
const paginatedPosts = allPosts.slice(
  (page - 1) * POSTS_PER_PAGE,
  page * POSTS_PER_PAGE
);

type BlogPost = {
  title: string;
  link: string;
  pubDate: Date;
  source: string;
};
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			main {
				width: 100%;
				max-width: 1200px;
				margin: 0 auto;
				padding: 2.5rem 1.5rem;
				box-sizing: border-box;
			}
			
			.content-wrapper {
				max-width: 100%;
				margin: 0 auto;
				padding: 0;
				box-sizing: border-box;
			}
			
			.hero-section {
				/* text-align: center; */
				margin-bottom: 3rem;
				padding: 2rem 0;
				background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
				/* border: 1px solid var(--gray-light); */
				color: white;
				border-radius: 16px;
				margin-bottom: 2rem;
				padding-left: 2rem;
			}
			
			.hero-section h1 {
				font-size: 1rem;
				margin: 0;
				text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
			}
			
			.hero-section p {
				font-size: 1rem;
				margin: 1rem 0 0 0;
				opacity: 0.9;
			}
			
			.search-section {
				margin-bottom: 2rem;
			}
			
			.search-box {
				width: 100%;
				padding: 1rem;
				font-size: 1rem;
				border: 2px solid var(--gray-light);
				border-radius: 8px;
				transition: border-color 0.2s ease;
			}
			
			.search-box:focus {
				outline: none;
				border-color: var(--accent);
			}
			
			.search-results-info {
				margin-top: 1rem;
				color: var(--gray);
				font-size: 0.9rem;
			}
			
			.filters {
				display: flex;
				gap: 1rem;
				margin-bottom: 2rem;
				flex-wrap: wrap;
				align-items: center;
			}
			
			.filter-label {
				font-weight: 600;
				color: var(--gray-dark);
			}
			
			.filter-buttons {
				display: flex;
				gap: 0.5rem;
				flex-wrap: wrap;
			}
			
			.filter-button {
				padding: 0.5rem 1rem;
				background: white;
				border: 1px solid var(--gray-light);
				border-radius: 20px;
				text-decoration: none;
				color: var(--gray-dark);
				font-size: 0.9rem;
				transition: all 0.2s ease;
			}
			
			.filter-button:hover {
				background: var(--gray-light);
				border-color: var(--accent);
			}
			
			.filter-button.active {
				background: var(--accent);
				color: white;
				border-color: var(--accent);
			}
			
			.stats {
				display: flex;
				justify-content: center;
				gap: 2rem;
				margin-top: 1.5rem;
			}
			
			.stat-item {
				text-align: center;
			}
			
			.stat-number {
				font-size: 1.5rem;
				font-weight: bold;
				display: block;
			}
			
			.stat-label {
				font-size: 0.9rem;
				opacity: 0.8;
			}
			
			ul {
				display: grid;
				grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
				gap: 2rem;
				list-style-type: none;
				margin: 0;
				padding: 0;
			}
			
			.post-item {
				background: white;
				border-radius: 16px;
				padding: 0;
				overflow: hidden;
				box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
				transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
				border: 1px solid rgba(0, 0, 0, 0.06);
				position: relative;
				display: flex;
				flex-direction: column;
			}
			
			.post-item::before {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				height: 4px;
				background: linear-gradient(90deg, var(--accent) 0%, #667eea 50%, var(--accent) 100%);
				background-size: 200% 100%;
				animation: shimmer 3s ease-in-out infinite;
			}
			
			@keyframes shimmer {
				0%, 100% { background-position: 0% 50%; }
				50% { background-position: 100% 50%; }
			}
			
			.post-item:hover {
				transform: translateY(-8px) scale(1.02);
				box-shadow: 0 12px 40px rgba(35, 55, 255, 0.15);
				border-color: var(--accent);
			}
			
			.post-item a {
				display: flex;
				flex-direction: column;
				height: 100%;
				text-decoration: none;
				color: inherit;
				padding: 1.75rem;
			}
			
			.post-item-header {
				margin-bottom: 1rem;
			}
			
			.post-meta-badge {
				display: inline-flex;
				align-items: center;
				gap: 0.5rem;
				padding: 0.4rem 0.9rem;
				background: linear-gradient(135deg, rgba(35, 55, 255, 0.1) 0%, rgba(102, 126, 234, 0.1) 100%);
				border-radius: 20px;
				font-size: 0.8rem;
				font-weight: 600;
				color: var(--accent);
				margin-bottom: 1rem;
			}
			
			.title {
				margin: 0 0 0.75rem 0;
				color: var(--black);
				line-height: 1.3;
				font-size: 1.25rem;
				font-weight: 700;
				transition: color 0.2s ease;
			}
			
			.post-item:hover .title {
				color: var(--accent);
			}
			
			.post-description {
				color: var(--gray-dark);
				font-size: 0.95rem;
				line-height: 1.6;
				margin: 0 0 1.25rem 0;
				flex-grow: 1;
			}
			
			.post-footer {
				display: flex;
				align-items: center;
				justify-content: space-between;
				padding-top: 1rem;
				border-top: 1px solid var(--gray-light);
				margin-top: auto;
			}
			
			.date {
				margin: 0;
				color: var(--gray);
				font-size: 0.85rem;
				font-weight: 500;
			}
			
			.read-more {
				color: var(--accent);
				font-size: 0.9rem;
				font-weight: 600;
				opacity: 0;
				transform: translateX(-10px);
				transition: all 0.3s ease;
			}
			
			.post-item:hover .read-more {
				opacity: 1;
				transform: translateX(0);
			}
			
			.source-badge {
				display: inline-block;
				padding: 0.2rem 0.6rem;
				background: var(--accent);
				color: white;
				border-radius: 20px;
				font-size: 0.8rem;
				margin-left: 0.5rem;
				font-weight: 500;
			}
			
			.source-badge.medium {
				background: #00ab6c;
			}
			
			.source-badge.local {
				background: #007acc;
			}
			
			ul li a:hover h4,
			ul li a:hover .date {
				color: var(--accent);
			}
			ul a:hover img {
				box-shadow: var(--box-shadow);
			}
			
			.pagination {
				display: flex;
				justify-content: center;
				gap: 1rem;
				margin-top: 3rem;
			}
			
			.pagination a {
				padding: 0.5rem 1rem;
				background: #f8f9fa;
				border-radius: 6px;
				text-decoration: none;
				color: var(--black);
				transition: all 0.2s ease;
			}
			
			.pagination a:hover {
				background: var(--accent);
				color: white;
			}

			.blog-preview-date {
				font-size: 0.85rem;
				color: var(--gray);
				margin: 0;
				font-style: italic;
			}

			@media (max-width: 720px) {
				main {
					padding: 1.5rem 1rem;
				}
				
				.hero-section {
					padding: 1.5rem 1rem;
					margin-bottom: 2rem;
				}
				
				.hero-section h1 {
					font-size: 1.5rem;
					margin-bottom: 0.75rem;
				}
				
				.hero-section p {
					font-size: 0.9rem;
					margin: 0.75rem 0 0 0;
				}
				
				.search-section {
					margin-bottom: 1.5rem;
				}
				
				.search-box {
					padding: 0.75rem;
					font-size: 0.9rem;
				}
				
				.filters {
					margin-bottom: 1.5rem;
					gap: 0.75rem;
				}
				
				.filter-label {
					font-size: 0.9rem;
				}
				
				.filter-button {
					padding: 0.4rem 0.75rem;
					font-size: 0.8rem;
				}
				
				.stats {
					flex-direction: column;
					gap: 0.75rem;
					padding: 1rem;
					margin-bottom: 1.5rem;
				}
				
				.stat-number {
					font-size: 1.5rem;
				}
				
				.stat-label {
					font-size: 0.8rem;
				}
				
				ul {
					grid-template-columns: 1fr;
					gap: 1rem;
				}
				
				.post-item {
					width: 100%;
				}
				
				.post-item a {
					padding: 1.25rem;
				}
				
				.post-item-header {
					margin-bottom: 0.75rem;
				}
				
				.post-meta-badge {
					padding: 0.3rem 0.75rem;
					font-size: 0.75rem;
					margin-bottom: 0.75rem;
				}
				
				.title {
					font-size: 1.1rem;
					margin-bottom: 0.5rem;
				}
				
				.post-description {
					font-size: 0.875rem;
					margin-bottom: 1rem;
				}
				
				.post-footer {
					padding-top: 0.875rem;
				}
				
				.date {
					font-size: 0.8rem;
				}
				
				.read-more {
					font-size: 0.8rem;
				}
				
				.pagination {
					margin-top: 2rem;
					gap: 0.5rem;
					flex-wrap: wrap;
				}
				
				.pagination a {
					padding: 0.4rem 0.75rem;
					font-size: 0.875rem;
				}
			}
			
			@media (max-width: 480px) {
				main {
					padding: 1.25rem 0.875rem;
				}
				
				.hero-section {
					padding: 1.25rem 0.75rem;
					margin-bottom: 1.5rem;
				}
				
				.hero-section h1 {
					font-size: 1.35rem;
				}
				
				.hero-section p {
					font-size: 0.85rem;
				}
				
				.search-box {
					padding: 0.625rem;
					font-size: 0.85rem;
				}
				
				.filter-button {
					padding: 0.35rem 0.625rem;
					font-size: 0.75rem;
				}
				
				.post-item a {
					padding: 1rem;
				}
				
				.title {
					font-size: 1rem;
				}
				
				.post-description {
					font-size: 0.8rem;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<div class="content-wrapper">
			<!-- Hero Section -->
			<div class="hero-section">
				<h1>📚 My Dev Notebook</h1>
				<p>Here, I share what I learn — one topic at a time.
				   Each post reflects my understanding, experiences, and takeaways as I explore new ideas and challenges</p>
			</div>
			
			<!-- Search Section -->
			<div class="search-section">
				<input 
					type="search" 
					id="blog-search" 
					class="search-box" 
					placeholder="Search posts by title, description, tags, or category..."
					autocomplete="off"
				/>
				<div id="search-results-info" class="search-results-info" style="display: none;"></div>
			</div>
			
			{/* Category Filters */}
			{sortedCategories.length > 0 && (
				<div class="filters">
					<span class="filter-label">Filter by category:</span>
					<div class="filter-buttons">
						<a 
							href="/blog" 
							class={`filter-button ${!categoryFilter ? 'active' : ''}`}
						>
							All ({allLocalPosts.length})
						</a>
						{
							sortedCategories.map(([category, count]) => (
								<a 
									href={`/blog?category=${encodeURIComponent(category)}`}
									class={`filter-button ${categoryFilter.toLowerCase() === category.toLowerCase() ? 'active' : ''}`}
								>
									{category} ({count})
								</a>
							))
						}
					</div>
				</div>
			)}

			<section>
				<ul id="posts-list">
					{
					paginatedPosts.map((post: BlogPost) => {
						// Get full post data for description
						const fullPost = localPostsMap.get(post.link);
						return (
							<li class="post-item" data-post-id={post.link}>
								<a href={post.link} target={post.source === 'medium' ? "_blank" : "_self"}>
									<div class="post-item-header">
										{fullPost?.category && (
											<span class="post-meta-badge">{fullPost.category}</span>
										)}
										<h4 class="title">{post.title}</h4>
									</div>
									{fullPost?.description && (
										<p class="post-description">{fullPost.description}</p>
									)}
									<div class="post-footer">
										<time class="date">
											<FormattedDate date={post.pubDate} />
										</time>
										<span class="read-more">Read →</span>
									</div>
								</a>
							</li>
						);
					})
					}
				</ul>

				<nav class="pagination">
					{Array.from({ length: totalPages }, (_, i) => (
					  <a
						href={`?page=${i + 1}`}
						style={page === i + 1 ? "font-weight: bold; background: var(--accent); color: white;" : ""}
					  >
						{i + 1}
					  </a>
					))}
				</nav>
			</section>
			</div>
		</main>
		<Footer />
		
		<script is:inline define:vars={{ searchData }}>
			(function() {
				// Load Fuse.js from CDN
				const script = document.createElement('script');
				script.src = 'https://cdn.jsdelivr.net/npm/fuse.js@7.0.0/dist/fuse.min.js';
				script.onload = function() {
					const searchInput = document.getElementById('blog-search');
					const postsList = document.getElementById('posts-list');
					const searchResultsInfo = document.getElementById('search-results-info');
					const originalPosts = Array.from(postsList?.children || []);
					
					if (!searchInput || !postsList || !searchResultsInfo) return;
					
					// Initialize Fuse.js
					const fuse = new Fuse(searchData, {
						keys: ['title', 'description', 'tags', 'category', 'type'],
						threshold: 0.3,
						includeScore: true,
					});
					
					// Search function
					function performSearch(query) {
						if (!query.trim()) {
							// Show all posts
							postsList.innerHTML = '';
							originalPosts.forEach(post => postsList.appendChild(post.cloneNode(true)));
							searchResultsInfo.style.display = 'none';
							return;
						}
						
						const results = fuse.search(query);
						
						postsList.innerHTML = '';
						
						if (results.length === 0) {
							postsList.innerHTML = '<li style="padding: 2rem; text-align: center; color: var(--gray);">No posts found matching your search.</li>';
							searchResultsInfo.textContent = `No results for "${query}"`;
							searchResultsInfo.style.display = 'block';
						} else {
							results.forEach(result => {
								const post = result.item;
								const li = document.createElement('li');
								li.className = 'post-item';
								const date = new Date(post.pubDate);
								li.innerHTML = `
									<a href="${post.link}">
										<h4 class="title">${post.title}</h4>
										<time class="blog-preview-date">${date.toLocaleDateString()}</time>
									</a>
								`;
								postsList.appendChild(li);
							});
							searchResultsInfo.textContent = `Found ${results.length} ${results.length === 1 ? 'post' : 'posts'} for "${query}"`;
							searchResultsInfo.style.display = 'block';
						}
					}
					
					// Debounce search
					let searchTimeout;
					searchInput.addEventListener('input', (e) => {
						clearTimeout(searchTimeout);
						const query = e.target.value;
						searchTimeout = setTimeout(() => performSearch(query), 300);
					});
				};
				document.head.appendChild(script);
			})();
		</script>
	</body>
</html>